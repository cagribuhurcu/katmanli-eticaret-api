@model IEnumerable<StokProject.Entities.Entities.Supplier>

@{
    ViewData["Title"] = "Index";
}

<div class="col-lg-12">
    <div class="card">
        <div class="card-body">
            <div class="custom-tab">
                <nav>
                    <div class="nav nav-tabs" id="nav-tab" role="tablist">
                        <a class="nav-item nav-link active" id="custom-nav-home-tab" data-toggle="tab" href="#custom-nav-home" role="tab" aria-controls="custom-nav-home" aria-selected="true">Aktif Tedarikçiler</a>
                        <a class="nav-item nav-link" id="custom-nav-profile-tab" data-toggle="tab" href="#custom-nav-profile" role="tab" aria-controls="custom-nav-profile" aria-selected="false">Tüm Tedarikçiler</a>
                    </div>
                </nav>
                <div class="tab-content pl-3 pt-2" id="nav-tabContent">
                    <div class="tab-pane fade show active" id="custom-nav-home" role="tabpanel" aria-labelledby="custom-nav-home-tab">
                        <div class="card">
                            <div class="card-header d-flex justify-content-between">
                                <strong class="card-title">Aktif Tedarikçiler</strong>
                                <a asp-area="Admin" asp-controller="Supplier" asp-action="AddSupplier" class="btn btn-success">Tedarikçi Ekle</a>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <thead class="thead-dark">
                                        <tr>
                                            <th scope="col">#</th>
                                            <th scope="col">Tedarikçi Adı</th>
                                            <th scope="col">Telefon</th>
                                            <th scope="col">Email</th>
                                            <th scope="col">İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @{
                                            int rowCount1 = 1;
                                        }
                                        @foreach (Supplier item in Model)
                                        {
                                            if (item.IsActive)
                                            {
                                                <tr>
                                                    <th scope="row">@rowCount1</th>
                                                    <td>@item.SupplierName</td>
                                                    <td>@item.Phone</td>
                                                    <td>@item.Email</td>
                                                    <td>
                                                        <a asp-area="Admin" asp-controller="Supplier" asp-action="UpdateSupplier" asp-route-id="@item.ID" class="btn btn-sm btn-warning">
                                                            Güncelle
                                                        </a>
                                                        <a asp-area="Admin" asp-controller="Supplier" asp-action="DeleteSupplier" asp-route-id="@item.ID" class="btn btn-sm btn-danger">
                                                            Sil
                                                        </a>
                                                    </td>
                                                </tr>
                                                rowCount1++;
                                            }
                                        }
                                    </tbody>
                                </table>

                            </div>
                        </div>
                    </div>
                    <div class="tab-pane fade" id="custom-nav-profile" role="tabpanel" aria-labelledby="custom-nav-profile-tab">
                        <div class="card">
                            <div class="card-header d-flex justify-content-between">
                                <strong class="card-title">Tedarikçiler</strong>
                                <a asp-area="Admin" asp-controller="Supplier" asp-action="AddSupplier" class="btn btn-success">Tedarikçi Ekle</a>
                            </div>
                            <div class="card-body">
                                <table class="table">
                                    <thead class="thead-dark">
                                        <tr>
                                            <th scope="col">#</th>
                                            <th scope="col">Tedarikçi Adı</th>
                                            <th scope="col">Telefon</th>
                                            <th scope="col">Email</th>
                                            <th scope="col">İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @{
                                            int rowCount = 1;
                                        }
                                        @foreach (Supplier item in Model)
                                        {

                                            <tr>
                                                <th scope="row">@rowCount</th>
                                                <td>@item.SupplierName</td>
                                                <td>@item.Phone</td>
                                                <td>@item.Email</td>
                                                <td>
                                                    <a asp-area="Admin" asp-controller="Supplier" asp-action="UpdateSupplier" asp-route-id="@item.ID" class="btn btn-sm btn-warning">
                                                        Güncelle
                                                    </a>
                                                    @if (item.IsActive)
                                                    {
                                                        <a asp-area="Admin" asp-controller="Supplier" asp-action="DeleteSupplier" asp-route-id="@item.ID" class="btn btn-sm btn-danger">
                                                            Sil
                                                        </a>
                                                    }
                                                    else
                                                    {
                                                        <a asp-area="Admin" asp-controller="Supplier" asp-action="ActivateSupplier" asp-route-id="@item.ID" class="btn btn-sm btn-primary">
                                                            Aktive Et
                                                        </a>
                                                    }
                                                </td>
                                            </tr>
                                            rowCount++;
                                        }
                                    </tbody>
                                </table>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
